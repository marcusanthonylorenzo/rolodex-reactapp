## Rolodex Style React App

### CREATE, READS & DELETES (No UPDATE at the moment.) VIA REACT ROUTER
  -Utilizes React Router/Routes (Switch is deprecated?)
  -Searches Contact Details via .filter and id (with uuid)
